# Phase 3 Plan: Hardening And Real-Repo Capability

## Handoff Summary

This file is the Phase 3 roadmap. Phase 2 finished with the harness able to
safely run a full task lifecycle on small workspaces; Phase 3 turns that
foundation into something we can point at real repositories with confidence.

Items 1–8 are in flight (most have committed scaffolding plus open
sub-bullets); items 9–10 are not started. Each item's per-section block
below has a `Status:` line and explicit `[x]` / `[~]` / `[ ]` boxes for
the sub-bullets. The high-level checklist immediately below mirrors that.

## Resume Point For The Next Agent

- **Latest completed slice:** signature-change impact in the
  pre-patch planner.
- **Test baseline:** `npm test` → 220 tests pass (0 skipped, 0
  failing); `npm run test:e2e` → 15 e2e tests pass.
- **Last two slices in order:**
  1. `component_map()` — `src/code/code-index.js` detects common
     React function/class/arrow component declarations and links JSX
     tags to local/imported components; runtime/model tools and phase
     allowed-tools expose it.
  2. Signature-change impact — `buildPrePatchPlan` scans for
     parameter / argument / signature / return-shape requests, stores
     `expected_scope.signature_impact`, adds affected definitions and
     callers to candidate files, and emits `signature_impact`
     warnings before patching.

- **Recommended next slice:** the remaining item 8 resolver gaps:
  tsconfig-paths, CommonJS/re-export/dynamic-import coverage, and
  Python imports.

Current status:

- [~] Real-repo fixtures and end-to-end CLI coverage (item 1) — almost
  done. In place: e2e CLI driver (with an early-exit rejection if the
  harness crashes mid-expect), fixture-copy helper, four fixtures
  (non-git plain, Node passing, Node failing, pytest failing), pluggable
  model adapter factory (`src/model/index.js`) plus a stub adapter and
  script helper (`test/e2e/helpers/stub-adapter.js`, `stub-script.js`),
  and 15 e2e tests covering banner+exit, full lifecycle, failing-verify
  path, non-git workspace, destructive command block, malformed patch
  reject, stub-driven file create with verify, dependency-change approval
  (denied), secret-file approval (denied), external-publish (push)
  approval (denied), model.respond throwing → graceful fallback, shadow
  apply-back conflict (blocked when real workspace changes during the
  task), and pytest runner recording a failed Python test (gated on
  `python -m pytest --version`). The work surfaced and fixed two real
  bugs: the permission engine's destructive regex used a trailing `\b`
  that let `rm -rf /` slip through, and `model.respond` errors used to
  crash the task by hanging the process via readline. Remaining
  sub-bullets: Vitest / Express+typecheck / Next.js+build fixtures
  (which require committing node_modules or shimming the toolchain) and
  transient-retry / fallback-model coverage (which is OpenRouter-adapter
  internal behavior; needs a fake-fetch test).
- [~] Stronger targeted checks and parsers (item 2) — reporter-aware
  parsing is wired end-to-end for the test runners we have parsers
  for, and build-error parsers now cover all seven targeted tools.
  `src/checks/structured-reporter.js` has parsers for TAP v13 (Node
  `--test`), Vitest JSON, Jest JSON, pytest JUnit XML, and ESLint
  JSON, each returning the same shape as `parseCheckOutput` (with a
  `parsed_source` tag added). The Vitest, Jest, and Node-test runner
  descriptors expose a `structuredReporter` object with reporter-
  flagged command builders; `runCheckCommand` accepts a
  `structuredFormat` option and falls back to the regex parser when
  structured parsing returns null. `parseCheckOutput` itself now has
  collectors for esbuild, Vite/Rollup, webpack, Next.js, TypeScript
  pretty mode, Cargo, and Go build output, plus the original
  TypeScript `(line,col)` form, ESLint, and test-runner forms. Golden
  fixtures live under `test/fixtures/check-output/` (six structured-
  reporter samples) and `test/fixtures/check-output/build-errors/`
  (seven build-tool samples). Failed-test → source mapping via the
  code index is now done in `src/checks/relevant-files.js` and wired
  into `runCheckCommand`; every check record carries a re-ranked
  `likely_relevant_files` plus a `likely_relevant_files_provenance`
  map (`stack` / `import-graph` / `co-located`). The repair loop
  summarises failures via `src/checks/failure-summary.js` before
  handing them to `model.repair`, so the model sees a compact
  structured shape instead of the full audit record. Still TBD for
  item 2: pytest JUnit wiring (needs a tmp-file capture since pytest
  does not write JUnit to stdout) and ESLint structured wiring.
- [~] Pre-patch blast radius and edit preview (item 3) — most of the
  surface is in place. `src/task/pre-patch-plan.js`'s
  `buildPrePatchPlan` produces an `expected_scope` (candidate files,
  risk labels, predicted checks), `danger_zones` (avoid_touching from
  memory, secret paths, lockfiles, dependency manifests), and
  `warnings` array. Each warning carries a `tier`, `severity`, and an
  optional `blocking: true` flag set when the planner can prove the
  candidate file set crosses a danger-zone path. The CLI's plan phase
  builds the plan, persists `pre-patch-plan.json` to the task dir,
  and (after the alignment pass) prompts the user only when at least
  one warning is `blocking: true`. Operation-tier warnings are
  prompted later when the operation actually runs, so the up-front
  card is no longer always-on. The phase controller requires
  `pre_patch_plan` as a plan-phase output. The runtime exposes
  `previewPatch(patch)` (surfaced as the `preview_patch` model tool).
  A `/plan` CLI shortcut prints the persisted plan. Rename and
  signature impact now ripple through the import graph via item 8.
  Still TBD for item 3: a "preview" review action that shows the next
  staged patch before apply.
- [~] Streaming wired into the CLI (item 4) — foundation in place.
  `requestOpenRouter` routes through a new `streamOpenRouterChat`
  helper when `modelConfig.capabilities.streaming` is true and the
  caller passes `onToken`. The helper accumulates SSE deltas (text +
  tool_call deltas by index) and reassembles them into the same
  shape as the non-streaming path so the existing tool-dispatch
  loop is unchanged. `respond` now accepts `onToken` and an
  `AbortSignal`; the CLI passes a per-task `AbortController`, prints
  tokens as they arrive, and `cancel task` aborts the in-flight
  request. Aborted requests surface as `{ cancelled: true, ... }`
  with a `model_aborted` event recorded. Locked in by three new
  unit tests (token-and-tool-call reassembly, fallback when
  capabilities.streaming=false, mid-stream abort). Still TBD: a
  streaming-aware `ui.assistant` variant that renders tokens
  inline rather than as a progress message; streaming the repair
  loop's model calls; and surfacing tool-call announcements as a
  thinking spinner.
- [~] Multi-provider model adapters (item 5) — most providers now
  land. The OpenAI-compatible transport in `src/model/openrouter.js`
  is parameterised by `endpoint` and `extraHeaders`, so OpenAI and
  any local server speaking the same wire format share the same code
  via thin wrappers (`src/model/openai.js`,
  `src/model/local.js`). Anthropic has its own implementation in
  `src/model/anthropic.js` against the Messages API directly (no
  SDK dep). `createModelAdapter` in `src/model/index.js` dispatches
  by `modelConfig.provider` (`openrouter` | `openai` | `anthropic` |
  `local`); the existing `LAMP_MODEL_ADAPTER` test-stub override
  still wins. Locked in by 7 new unit tests covering factory
  dispatch, the adapter contract surface, OpenAI / local URL +
  header behaviour, and Anthropic tool-translation. Still TBD:
  prompt caching for Anthropic (`anthropic-beta:
  prompt-caching-2024-07-31`), Anthropic streaming inside
  `respond`/`repair`, and a live-network smoke test gated on
  per-provider env vars.
- [~] Structured outputs beyond critique (item 6) — plan and
  edit-spec land. `src/model/structured-output.js` defines
  `PLAN_SCHEMA`, `EDIT_SPEC_SCHEMA`, and `REPAIR_FINDINGS_SCHEMA`
  plus a small JSON-Schema-shaped `validate()` and a
  `describeSchema()` helper used to build system prompts. Each
  adapter (OpenAI-compatible and Anthropic) gained a `respondJson({
  system, user })` method that returns `{ok, structured?, raw}`.
  `src/task/structured-plan.js` and `src/task/edit-spec.js`
  orchestrate the requests, validate the response, persist
  `plan.json` / `edit-spec.json` under the task dir on success, and
  silently fall back when the model is unavailable, the response
  isn't JSON, or schema validation fails. After the alignment pass
  the CLI only fires these when the task crosses a risky boundary
  (`riskyBoundaries.length > 0`) — they cost a model round-trip
  each, and on routine no-risk tasks they were not load-bearing.
  Still TBD: a structured repair-findings flow (the schema is
  defined; the wiring is not), and live-network smoke tests.
- [x] Resumable tasks and cancel-in-flight (item 7) — complete for
  artifact-backed resume. Phase controller gained
  `markCancelled` and `markInterrupted` helpers; `cancelTask`
  flips the in-progress phase to `cancelled` (or `interrupted` for
  Ctrl-C) before flipping the task status. A SIGINT handler in
  `src/index.js` captures partial state on Ctrl-C and exits with
  code 130. New CLI commands `/tasks` (lists recent tasks with
  status, last phase, resumable flag) and `/show <task-id>` (full
  task summary). Approval denials with "another approach" now
  append a `constraint`-typed belief so the model sees the
  boundary on its next iteration. `/resume <task-id>` now loads a
  recorded task and continues at the next incomplete phase using
  persisted task artifacts. Remaining limitation: it does not
  resurrect a deleted temp shadow workspace after a process restart.
- [~] Dependency-aware edits and cross-file binding (item 8) — six
  slices land (cross-file binding, rename impact, repair import graph,
  dependency graph, component map, signature-change impact).
  `src/code/import-resolver.js` is the new shared resolver: relative-import sources resolve against the workspace
  file set with the standard `.ts/.tsx/.js/.jsx/.mjs/.cjs` extension
  list and an `index.<ext>` fallback; `src/checks/relevant-files.js`
  was rewritten to call it instead of duplicating the logic.
  `findSymbolCallers({ cwd, codeIndex, symbol })` and
  `findSymbolDependencies({ codeIndex, file })` in
  `src/code/code-index.js` walk the imports map: callers respect
  named, aliased (`import { foo as bar }`), default, and namespace
  imports, and only return files that import the symbol from one of
  its actual defining files (not just any file with a same-named
  export). Reference scanning uses the local alias, not the
  exported name. Both functions are exposed as
  `tools.findSymbolCallers` / `tools.findSymbolDependencies` and as
  the `symbol_callers` / `symbol_dependencies` model tools. Triage
  and patch `allowedTools` were updated to admit them. The
  pre-patch plan now uses a sync `listSymbolImpact` helper to
  detect rename intent and surface a `rename_impact` warning
  (blocking when the symbol has cross-file callers, info when
  only the definition is affected); affected files land in
  `expected_scope.candidate_files` so they participate in the
  same danger-zone crosses as keyword candidates. Locked in by
  `test/import-resolver.test.js` (7 tests),
  `test/symbol-callers.test.js` (9 tests), and 4 new
  rename-impact tests in `test/pre-patch-plan.test.js`.
  `dependency_graph(path?)` is wired through runtime/model tools and
  phase allowed-tools, returning whole-workspace nodes, internal
  edges, external imports, and focused path subgraphs with direct
  dependents. `component_map()` is wired through the same runtime/model
  and phase surfaces, returning best-effort React component
  declarations and JSX render edges. Signature-change impact is now
  wired into `buildPrePatchPlan`: parameter / argument / return-shape
  requests populate `expected_scope.signature_impact`, add definition
  and caller files to candidate scope, and block before patch when the
  symbol has cross-file callers. Still open: tsconfig-paths alias
  resolution and Python `from x import y` resolution.
- [ ] Real language-server integration, TypeScript first (item 9) — not started.
- [ ] GitHub/CI integration (item 10) — not started.

Where we are now:

- Latest verified baseline: `npm test` passes with 220 tests
  (0 skipped, 0 failing). `npm run test:e2e` runs 15 end-to-end
  tests, all passing.
- The Phase 2 baseline (seven-phase lifecycle, structured task
  artifacts, snapshot-based undo, shadow workspace, regex
  code-intel, targeted check runner, bounded repair loop, styled
  review card) carried forward unchanged.
- `.agent/` is intentionally ignored runtime state. All Phase 3
  items must keep the harness usable when `.agent/` is absent or
  partial.

Useful verification commands:

```sh
npm test           # full suite (unit + e2e)
npm run test:e2e   # e2e only
```

## Current Baseline

Carried forward from Phase 2:

- CLI chat shell with styled output, interactive menus, approval prompts, and
  review actions (`accept`, `adjust`, `undo`, `see diff`, `see technical
  details`, `open changed file list`, `resolve conflicts`, `cancel task`).
- Task state under `.agent/tasks/<id>/` with task.json, beliefs.json,
  changed-files.json, commands.jsonl, model-usage.jsonl, check-results.json,
  checks/*, snapshots/*, conflicts/*, phases.json, verification.json,
  apply-back.json, apply-back-conflicts.json, review.md, final-summary.md,
  events.jsonl.
- Project memory under `.agent/memory/project.json` with stale-source refresh
  preserving `decisions` and `avoid_touching`.
- Permission engine that classifies paths and commands into allow/ask/blocked
  tiers, with explicit handling for dependency, network, secret,
  outside-workspace, push/deploy, destructive, local-check, and read-only forms.
- Tool runtime: list/read/search, edit primitives (`replace_range`,
  `replace_exact`, `insert_before/after`, `create_file`, `delete_file`,
  `rename_file`), `apply_patch`, `run_command`, broad and targeted check
  runners, code intelligence (`find_symbols`, `find_definition`,
  `find_references`, `find_imports`, `find_exports`, `route_map`,
  `symbol_callers`, `symbol_dependencies`, `dependency_graph`,
  `component_map`).
- Shadow workspace via git worktree or filtered temp copy, with apply-back,
  hash-based conflict detection, and per-file keep/apply/save resolution.
- Bounded verify-and-repair loop (3 attempts) that prefers targeted runs when
  failed files are known.
- Critique pass with structured findings (model JSON when available, local
  fallback otherwise).
- Final review card with changed-file reasons, blast radius, check snippets,
  severity-grouped warnings, and task timeline.
- Model adapter contract (`respond`, `repair`, `critique`, `streamText`,
  `capabilities`) with OpenRouter implementation, fallback models, transient
  retry, usage/cost tracking, and a structured-JSON critique mode.

Phase 3 focuses on three things on top of that foundation:

- **Confidence on real repos** via fixtures and end-to-end coverage so we can
  measure regressions when we change anything else.
- **Better signal** from checks, blast radius, and cross-file binding so the
  agent works on the right files and sees what an edit will break before it
  patches.
- **Productization** of streaming, multi-provider adapters, resumable tasks,
  cancel-in-flight, and GitHub/CI so the harness behaves like a tool a person
  would actually leave running on a real codebase.

## What Is Left

Items 1–8 are partially done — see the per-item sections below for the
exact `[x]` / `[~]` / `[ ]` state. Items 9–10 are not yet started.

Recommended next steps in priority order:

1. Remaining item 8 leftovers: tsconfig-paths alias resolution
   (extend `src/code/import-resolver.js`), Python
   `from x import y` resolution, and re-export / dynamic-import
   coverage tests.
2. Item 6 leftover: wire `repair_findings` (schema is defined in
   `src/model/structured-output.js`) into `verifyAndRepair` and
   the review surface.
3. Item 5 leftovers: Anthropic prompt caching
   (`anthropic-beta: prompt-caching-2024-07-31`), Anthropic
   streaming inside `respond`/`repair`, live-network smoke test.
4. Item 4 leftover: streaming-aware `ui.assistant`, streaming
   the repair loop's model calls.
5. Item 3 leftover: a "preview" review action that shows the next
   staged patch before apply.
6. Item 2 stragglers: pytest JUnit wiring (needs tmp-file
   capture, since pytest doesn't write JUnit to stdout) and
   ESLint structured wiring.
7. Items 9 (real LSP integration) and 10 (GitHub/CI integration)
   are sized as their own future rounds.

---

## 1. Real-Repo Fixtures And End-To-End CLI Coverage

Status: in progress. Foundation landed; failure-mode coverage and the
remaining fixture variants are still open.

### Goal

Move from unit tests over isolated functions to a fixture-driven suite that
exercises the full CLI lifecycle against representative real-world repositories.

### Add

- [~] `test/fixtures/` repos covering at least:
  - [ ] a small Node project with Vitest and a deliberately failing test
  - [x] a small Node project using `node:test` with a passing suite
        (`node-builtin-test-passing`)
  - [x] a small Node project using `node:test` with a deliberately failing
        suite (`node-builtin-test-failing`)
  - [ ] a small Express-like API with a typecheck failure
  - [ ] a small Next.js app with a build-time error
  - [x] a small Python project with a pytest failure (`pytest-failing`,
        with `pyproject.toml` configuring pytest to recognise
        `check_*.py` so Node's parent test runner does not auto-discover
        Python files; e2e test gated on `python -m pytest --version`)
  - [x] a non-git plain directory (`non-git-plain`, exercises snapshot diff
        and the no-package-manager fallback)
- [x] `test/e2e/cli.test.js` driver that spawns `node ./src/index.js` with a
  scripted stdin and asserts on stdout and on artifacts written under
  `.agent/tasks/`. Driver lives in `test/e2e/helpers/cli-driver.js`; fixtures
  are copied to a tmpdir per test by `test/e2e/helpers/copy-fixture.js`.
- [~] Golden assertions for: project triage summary, plan recorded before
  patch, verify-and-repair loop running, critique produced, review card text,
  shadow apply-back behavior, and conflict resolution path. Currently
  asserting: phase completion, task plan recorded, project memory written,
  check-results recorded, verification status (passed and failed paths),
  events emitted, final-summary card text, and checkpoint contents on
  non-git workspaces. Shadow apply-back + conflict resolution e2e still TBD.
- [~] Failure-mode coverage:
  - [x] destructive-command block (`rm -rf /`, asserts permission engine
        records `decision.action: "blocked"` and the command never runs)
  - [x] apply-patch reject (malformed unified diff returns error, no
        changed files tracked, fixture sources untouched)
  - [x] secret-file access prompt (`.env` read triggers approval; denied
        in non-interactive driver, file unread)
  - [x] dependency-change prompt (`npm install lodash` triggers approval;
        denied; commands.jsonl records `decision.tier: "dependency_change"`)
  - [x] external-publish prompt (`git push` triggers approval; denied;
        commands.jsonl records `decision.tier: "external_publish"`)
  - [x] provider error: stub `respond` throws a simulated 503; the
        harness logs a `model_error` event, surfaces a warning to the
        user, and continues into verify with a fallback assistant
        response so the task still reaches `final_review`.
  - [ ] transient retry / fallback model — OpenRouter-adapter internal
        behavior; needs a fake-fetch test rather than the e2e harness.
  - [x] shadow apply-back conflict (real workspace edited during a
        shadow-mode task; `accept` review action blocked;
        `apply-back-conflicts.json` written with the affected files;
        the user's out-of-band edit is preserved on disk).
- [x] Test helpers for stubbing the model adapter so e2e flows do not need
  network. `src/model/index.js` exposes `createModelAdapter(modelConfig)`
  which honors the `LAMP_MODEL_ADAPTER` env var (path to an ESM module
  exporting `createAdapter`); tests use this to inject
  `test/e2e/helpers/stub-adapter.js`, which reads a JSON script from
  `LAMP_STUB_SCRIPT` and runs the listed tools against the harness's
  `tools` object. `test/e2e/helpers/stub-script.js` exposes the absolute
  adapter path and a `writeStubScript()` helper.
- [x] CLI-driver `respondToApproval(answer)` for the typed-fallback
  approval prompt the harness writes when stdin is non-TTY and an
  approval boundary is hit. Used by the dependency-change, secret-file,
  and external-publish failure-mode tests.
- [x] `npm run test:e2e` script and a CI-friendly default that skips e2e if a
  fast loop is requested. `npm test` and `npm run test:e2e` both go through
  `scripts/run-tests.mjs`, which enumerates `*.test.{js,mjs,cjs}` files
  explicitly and skips `helpers/`/`fixtures/` to avoid auto-discovery
  pollution under Node 24.

### Bugs caught by this work

- The permission engine's destructive command regex used a trailing `\b`
  that failed to match at end-of-string, letting `rm -rf /` and
  `chmod -R 777 /` slip through into the "ask" tier. Fixed in
  `src/permissions/permission-engine.js` by splitting the regex into
  word-bounded alternatives (`sudo`, `format`, `del /s`, `Remove-Item`)
  and unbounded alternatives (`rm -rf /|~`, `chmod -R 777 /`). New unit
  tests in `test/permission-engine.test.js` lock the fix in.
- `src/index.js` did not catch errors from `model.respond`, so an
  adapter throwing (provider 5xx, network blip, stub exception) tore
  down the task: main rejected, the readline interface kept the process
  alive, and no further output appeared. Fixed by wrapping
  `model.respond` in a try/catch that logs a `model_error` event,
  prints a warning, and synthesises a fallback `assistant_response` so
  the lifecycle continues into verify, critique, and final_review.

### Notes for the next agent

- The CLI driver strips `NODE_TEST_*` env vars before spawning the harness.
  Without this, any `node --test` invocation the harness runs (for example,
  `npm test` inside a fixture) would attach to the parent test runner via
  the v8 child-test protocol and report exit code 0 on real failures.
- Fixture tests must not be named `*.test.{js,mjs,cjs}` and must not live
  inside directories named `test`/`tests`/`__tests__`, otherwise Node's
  auto-discovery picks them up as part of the parent suite. The current
  fixtures use `specs/main.mjs` invoked through an explicit file path in
  the fixture's `package.json` test script.
- The phase controller's `validatePatchReadiness` rejects a `patch` phase
  start unless either `task_type` matches `docs|setup|explain` or the
  triage step recorded at least one notable file. When writing e2e tests
  for non-trivial task types, prefer fixtures whose triage finds notable
  files (`package.json`, `src/`, `test/`, `README.md`, configs); the
  `node-builtin-test-passing` fixture is a safe default. If the harness
  exits mid-task because of a phase-gate throw, the readline interface
  keeps the process alive without producing further output, which the
  driver now handles via an `exit` listener inside `expect()`.
- The pluggable model adapter (`createModelAdapter` in `src/model/index.js`)
  is the seam that Phase 3 item 5 (multi-provider adapters) will plug new
  provider modules into. Each new provider should export `createAdapter`
  the same way the stub does.

### Definition Of Done

A new contributor can run `npm test` and `npm run test:e2e`, see the harness
drive each fixture repo through the whole task lifecycle, and trust that a
regression in any component is caught before it lands.

---

## 2. Stronger Targeted Checks And Parsers

Status: in progress. Reporter-aware parsers are in; wiring and build-error
depth remain.

### Goal

Turn check output into reliably structured failure data for every framework we
claim to support, and map failed tests back to the source files they exercise
so the repair loop can edit the right thing.

### Add

- [~] Reporter-aware runners: when the runner supports it, prefer JSON output
  (`vitest --reporter=json`, `jest --json`, `pytest --junit-xml`,
  `tsc --pretty false`, `eslint --format=json`) and parse the structured form
  before falling back to regex over stdout.
  - [x] `src/checks/structured-reporter.js` exposes `parseStructuredOutput`
        plus per-format `parseTap` / `parseVitestJson` / `parseJestJson` /
        `parsePytestJUnit` / `parseEslintJson` functions. Each returns the
        same `{ command, check_type, status, exit_code, failed_files,
        failed_tests, errors, stack_traces, expected, actual,
        likely_relevant_files, summary }` shape as `parseCheckOutput`,
        and returns `null` on input that does not look like its target
        format so callers can fall back to the existing regex parser.
  - [x] Golden-output fixtures in `test/fixtures/check-output/`: real
        captures (`node-tap-passing.txt`, `node-tap-failing.txt`,
        `pytest-junit-failing.xml`) plus hand-crafted samples
        (`vitest-failing.json`, `jest-failing.json`,
        `eslint-failing.json`, `eslint-passing.json`) and a README
        documenting how to refresh them.
  - [x] `test/structured-reporter.test.js` verifies each parser pulls
        the right `failed_tests`, `failed_files`, `stack_traces`,
        `expected`/`actual`, error severity, and rule names; verifies
        the dispatcher routes by format; verifies each parser returns
        `null` on malformed input.
  - [~] Wire into `runCheckCommand` so the harness picks the JSON/JUnit
        reporter when the runner supports it, parses the structured
        output first, and falls back to the regex `parseCheckOutput`
        when the reporter is unavailable.
        - [x] Vitest (`--reporter=json`), Jest (`--json`), and Node
              `--test` (`--test-reporter=tap`) wired through
              `runner.structuredReporter` and a new `structuredFormat`
              option on `runCheckCommand`. Records carry a
              `parsed_source` tag (`structured:<format>` or `regex`).
        - [ ] pytest JUnit wiring — defer until we have tmp-file
              capture, since pytest does not write JUnit XML to stdout.
        - [ ] ESLint JSON wiring — `runLint` currently invokes the
              user's lint script as-is; wiring needs either a separate
              targeted runner or careful in-place transformation.
- [x] Build-error depth: parsers for Vite/Rollup, esbuild, webpack, Next.js,
  TypeScript build mode (pretty), Cargo, and Go build output. Each
  produces the standard `errors[]` record (`{ source, file, line,
  column, severity, code?, message }` plus tool-specific fields like
  `plugin` and `kind`) and updates `failed_files`. Implementation in
  `src/checks/check-parser.js` (new collectors run alongside the
  existing TypeScript / ESLint / test collectors). Locked in by
  hand-crafted golden fixtures under
  `test/fixtures/check-output/build-errors/` (esbuild, vite, webpack,
  nextjs, typescript-pretty, cargo, go) and per-tool tests in
  `test/check-parser.test.js`, plus a composite test verifying
  parsers do not interfere with each other in the same output.
- [ ] Framework-specific failure shapes recorded in `check-results.json`:
  expected/actual snapshot diffs, mismatched type vs value errors, missing
  module resolution, syntax-error file/line, lint rule and fix availability.
- [x] Failed-test → source mapping: walk imports from the failing test file to
  rank candidate source files, and surface the ranked list under
  `likely_relevant_files` with provenance (`stack`, `import-graph`,
  `co-located`). Implementation in `src/checks/relevant-files.js`
  exposes `mapFailedTestsToSources(parsed, { codeIndex, allFiles, cwd })`
  that resolves relative imports against the workspace file list (with
  common extensions and `index.<ext>` fallback), applies a co-location
  heuristic that drops `.test.`/`.spec.`/`_test.` from filenames, and
  treats files inside `test/`/`tests/`/`__tests__/`/`spec/`/`specs/`
  as test files even when they don't carry the conventional suffix.
  `runCheckCommand` calls the mapper with the runtime's cached code
  index after parsing, so every persisted check record now carries
  both the re-ranked `likely_relevant_files` (import-graph >
  co-located > stack) and a `likely_relevant_files_provenance` map.
  Locked in by `test/relevant-files.test.js` (8 tests covering import
  resolution, co-location, bare-specifier skipping, Windows path
  normalisation, and the priority ordering) plus an integration test
  in `test/targeted-check-tools.test.js` that runs a real failing
  `node --test` and asserts `src/calculator.js` shows up with
  `import-graph` provenance.
- [x] Golden-output tests under `test/fixtures/check-output/` for each parser
  so future regressions show up as diffs.
- [x] Repair loop changes that prefer the new structured failure shape and
  pass it into `model.repair` instead of free-form `parsed` blobs.
  `src/checks/failure-summary.js` exposes
  `summarizeFailureForRepair(parsed)` which picks the model-useful
  fields (`check_type`, `status`, `errors[]`, `failed_files`,
  `failed_tests`, `expected`/`actual`, parsed source, and
  `likely_relevant_files` zipped with their provenance tags) and
  drops the audit-only fields (raw stdout/stderr paths, IDs,
  timestamps, redundant full-message strings). `repair-loop.js` calls
  this summariser before passing failures to `model.repair`. Locked
  in by 4 unit tests in `test/failure-summary.test.js` and an
  integration test in `test/repair-loop.test.js` that captures the
  argument `model.repair` actually receives.

### Notes for the next agent

- Node's TAP reporter labels its output `TAP version 13` even though the
  YAML diagnostic block uses TAP v14 conventions. Our parser accepts
  either label.
- Single-quoted YAML scalars in Node's TAP output emit `\\` for each
  backslash in paths. Our `stripYamlQuotes` decodes that one Node-style
  escape but deliberately does not decode `\n`/`\t` — applying those
  would corrupt Windows paths that contain `\t` (e.g.
  `...\test\fixtures\...`).
- Pytest's JUnit body uses `\r\n` line endings on Windows. The
  assertion-extraction regex must tolerate `\r?\n`.
- Wiring a runner into the structured path means three things: add a
  `structuredReporter` object to the runner descriptor in
  `test-runner-detector.js` with the format name and reporter-flagged
  command builders; ensure the format has a parser registered with
  `parseStructuredOutput` in `structured-reporter.js`; then any caller
  going through `pickRunnerCommand` in `runtime.js` will automatically
  prefer the structured form. The fallback to regex is automatic when
  the structured parser returns null, so a runner whose reporter
  output is malformed in practice degrades gracefully.

### Definition Of Done

For a failing Vitest, Jest, pytest, eslint, or tsc run, the harness records a
structured failure with file/line/column data and a ranked list of likely
relevant source files, and the repair loop edits one of those files first.

---

## 3. Pre-Patch Blast Radius And Edit Preview

Status: in progress. Plan, warning surface, edit-preview tool, and
phase gate are landed; review-action preview is still pending.

### Goal

Compute scope, risk, and verification expectations *before* the model patches,
not just at review time, so the model and the user can see what an edit will
touch.

### Add

- [x] Move blast-radius computation in `src/review/review-summary.js` into a
  pre-patch path that runs after triage and before patch.
  Implementation: `src/task/pre-patch-plan.js` builds an
  `expected_scope` from user-request keyword overlap, notable files,
  and danger-zone auto-adds.
- [x] New phase-level artifact: `pre-patch-plan.json` containing inferred
  affected files (from triage + workspace files), risk labels, predicted
  verification (which targeted checks should run), and a list of files the
  model is expected to touch.
- [x] Edit-preview tool: dry-run variants of `apply_patch` returning the
  resulting diff without writing. Surfaced as `tools.previewPatch` and
  the `preview_patch` model tool. The patch phase now allows
  `preview_patch` so the model can validate a patch before commit.
- [x] Pre-patch warning surface: when the planned edit set crosses
  `avoid_touching` from project memory, a secret path, a dependency manifest,
  or a checked-in lockfile, surface the warning and prompt for approval before
  the patch phase begins. Implemented via the `blocking: true` flag on
  warnings; only blocking warnings prompt, others print as
  informational context.
- [x] CLI: a `/plan` shortcut that prints the current `pre-patch-plan.json`.
  A "preview" review action that shows the next staged patch before
  apply is still pending.
- [x] Phase controller gate: `patch` cannot begin until the pre-patch plan is
  recorded. Enforced by adding `pre_patch_plan` to
  `TASK_PHASES.plan.requiredOutputs`.

### Notes for the next agent

- The CLI driver `respondToApproval(answer)` tracks an offset internally
  so consecutive calls each wait for the *next* prompt, not the stale
  tail of the previous one. This was needed once two approval prompts
  could fire in sequence (pre-patch warning + actual operation).
- `scanUserRequestSignals` deliberately splits its `.env` check out of
  the `\b`-bounded keyword set: `\b` does not match between two
  non-word characters (e.g. the space before `.env`), so the pattern
  has to use a separate alternative.
- The CLI passes the full workspace file list (not just
  `notableFiles`) into `buildPrePatchPlan` via the `codeIndex.files`
  argument — `notableFiles` filters out roots like `.env` that are
  important for the secret-file blocker.

### Definition Of Done

For any non-trivial task, the harness can answer "what files do you expect to
change, what could break, and which checks will you run?" before any byte is
written, and the user can preview a staged patch before it lands.

---

## 4. Streaming Wired Into The CLI

Status: in progress. Adapter, plumbing, and abort path are landed; the
remaining UI polish is on top of a working stream.

### Goal

Render the model's response progressively in the CLI, including during
tool-calling loops, instead of waiting for the full message before showing
anything.

### Add

- [x] `src/index.js` integration: when `capabilities.streaming` is true,
  the `respond` call passes an `onToken` callback that prints tokens to
  stdout as they arrive (with a one-line "Streaming model response"
  progress indicator on the first token, then a closing newline).
- [x] Streaming tool-call loop in `src/model/openrouter.js`: a new
  `streamOpenRouterChat` helper handles the SSE stream, accumulates
  text + tool-call deltas by index, and returns the same
  `{ choices, usage }` shape as the non-streaming call so the existing
  tool-dispatch loop in `respond` is unchanged.
- [ ] CLI rendering: a streaming-aware variant of `ui.assistant` that does not
  re-box on every token, plus a "thinking" status while tools run.
- [x] Backpressure and cancellation: every `respond` call accepts an
  `AbortSignal`. The CLI creates a per-task `AbortController` and
  `cancel task` aborts the controller. Aborted requests surface as
  `{ cancelled: true, ... }` with a `model_aborted` event recorded.
  `requestOpenRouter` propagates `AbortError` immediately rather than
  retrying.
- [x] Usage and cost recorded against the streamed call. The streaming
  path forwards `stream_options.include_usage` so OpenRouter returns
  usage in the final SSE chunk; `recordModelUsage` tags the entry with
  `streaming: true`.
- [x] Tests with a fake stream emitter exercising token order, tool-call
  delta reassembly across multiple SSE chunks, fallback when
  `capabilities.streaming=false`, and mid-stream cancellation.

### Notes for the next agent

- The streaming path uses `stream_options: { include_usage: true }` so
  OpenRouter emits a final chunk with `usage`. Without it, streamed
  calls would record `usage: null`.
- `AbortError` is detected via `error.name === "AbortError"` or the
  numeric `ABORT_ERR` / `20` codes (Windows / Node variants emit
  different shapes). It is propagated immediately; the retry loop
  bypasses it so a cancelled request does not silently retry.
- `respond` returns `{ cancelled: true }` when the abort fires; the
  CLI's caller treats this like any other model response and lets the
  task finish via verify/critique/final_review with the cancellation
  noted in `taskPatch.assumptions`.

### Definition Of Done

When streaming is enabled, the user sees the model's narration appear token by
token, sees tool calls announced as they happen, and can cancel mid-stream
without leaving a half-completed task.

---

## 5. Multi-Provider Model Adapters

Status: in progress. OpenAI / Anthropic / local adapters all land in
this round; `createModelAdapter` dispatches by provider; remaining
work is Anthropic-specific polish (caching, streaming-with-tools) and
a live-network smoke harness.

### Goal

Make provider choice a configuration concern rather than a code change, so the
harness can run against Anthropic, OpenAI, OpenRouter, and a local model
without rewriting task logic.

### Add

- [x] `src/model/anthropic.js` adapter implemented directly against
  the Messages API (no SDK dep). Supports tool calling (translates
  the harness's `TOOL_DEFINITIONS` into Anthropic `input_schema`),
  text-only streaming via `streamText`, and structured-JSON critique.
  Out of scope for this round: prompt caching and streaming inside
  `respond`/`repair`.
- [x] `src/model/openai.js` adapter for the OpenAI Chat Completions
  API (compatible with Azure OpenAI when `endpoint` is overridden).
- [x] `src/model/local.js` adapter for an OpenAI-compatible local
  endpoint (Ollama, LM Studio, vLLM, llama.cpp's `--api-server`).
  Requires `endpoint` or `baseUrl`.
- [x] Provider selection in `.agent/config.json` (`model.provider:
  "anthropic" | "openai" | "openrouter" | "local"`) with a single
  `createModelAdapter` factory in `src/model/index.js` that picks
  the right adapter. Defaults to `openrouter` for backward compat.
- [x] Capability negotiation: each adapter declares real
  `capabilities`; the OpenAI-compatible adapter now surfaces the
  caller's provider name in `capabilities().provider` instead of the
  hard-coded `"openrouter"` it used to emit when delegated to.
- [ ] Per-provider env var documentation in README (per-provider
  defaults are wired in `index.js`, but the README handoff section
  could grow a quick table).
- [x] Adapter contract tests (one per provider) verifying the
  `MODEL_ADAPTER_METHODS` surface and capability shape. See
  `test/multi-provider-adapters.test.js`.
- [ ] A live-network smoke test (skipped unless an env var is set)
  that runs one minimal request per provider against a small
  fixture.

### Notes for the next agent

- The OpenAI-compatible transport reads `endpoint` and
  `extraHeaders` from the model config. When `endpoint` (or
  `baseUrl`) is set, `extraHeaders` defaults to `{}` rather than
  the OpenRouter `HTTP-Referer`/`X-Title` pair — sending those to
  OpenAI or a local server would just leak the harness identifier
  unnecessarily.
- The Anthropic adapter reuses the harness's `TOOL_DEFINITIONS`
  catalog by translating each definition's `function.parameters`
  field into Anthropic's `input_schema`. If new tools are added,
  they automatically become available to all providers without
  any extra wiring.

### Definition Of Done

Switching provider in `.agent/config.json` is enough to retarget the harness;
all artifact paths, phase behavior, and review output are unchanged.

---

## 6. Structured Outputs Beyond Critique

Status: in progress. Plan and edit-spec land this round; repair-findings
schema is defined but not wired.

### Goal

Use structured outputs for more than critique so plans, edit specs, and repair
findings come back as data we can validate, not free-form text we have to
parse.

### Add

- [x] JSON-mode `plan` output: `PLAN_SCHEMA` defined in
  `src/model/structured-output.js`; the CLI plan phase requests a
  structured plan via `requestStructuredPlan` (uses adapter
  `respondJson`), validates it, and persists it as `plan.json` under
  the task dir. The heuristic plan stays as the authoritative
  `current_plan` for backward compat; the model plan augments it.
- [x] JSON-mode `edit_spec` output: `EDIT_SPEC_SCHEMA` defined; the
  CLI patch phase requests `requestStructuredEditSpec` BEFORE
  `model.respond` runs, validates, and persists `edit-spec.json`.
  The spec is informational at this stage — the model still does
  its real work via tool calls — but it gives the user a preview
  and the audit log a record of intent.
- [ ] JSON-mode `repair_findings` output: schema is defined
  (`REPAIR_FINDINGS_SCHEMA`), but the repair loop still returns the
  free-form summarised failure. Wiring it through is the next step.
- [x] Schema validation on every structured response. The validator
  in `src/model/structured-output.js` returns
  `{ ok, errors: [{ path, message }] }` and the helpers persist
  artifacts only when validation passes. On failure they emit a
  `structured_*_invalid` event and fall back silently.
- [x] Capability-gated: `requestStructured*` short-circuits with
  `source: "unsupported"` when the adapter has no `respondJson`,
  and the CLI guards on `config.model.allowNetwork`.
- [x] Test fixtures pinning each schema with an example response,
  plus fake-fetch tests for the two adapters' `respondJson`.

### Notes for the next agent

- The validator is a small JSON-Schema subset (string / integer /
  number / boolean / array / object / any) with `enum` and `pattern`
  on strings, and `required` on objects. If you need richer features
  (oneOf, anyOf, additionalProperties), grow the validator surgically
  rather than reaching for a dependency.
- The `parseRawJson` helper duplicated in OpenRouter and Anthropic
  adapters tolerates markdown fences and leading commentary. If a
  third adapter ever needs it, lift it into a shared module.

### Definition Of Done

Plan, edit spec, and repair output are validated structures persisted under
`.agent/tasks/<id>/`, viewable via `/details`, and used by other phases
without ad-hoc string parsing.

---

## 7. Resumable Tasks And Cancel-In-Flight

Status: complete for artifact-backed resume. Cancellation, Ctrl-C,
listing, alternate-approach beliefs, and `/resume <task-id>` are in
place.

### Goal

Treat tasks as durable objects that can be paused, resumed, or cancelled
mid-flight without leaving the harness in an inconsistent state.

### Add

- [x] Resume: a `/resume <task-id>` command loads `task.json` and
  `phases.json`, rejects non-resumable tasks, records
  `task_resumed`, and continues at the next not-completed phase
  using artifacts already on disk.
- [x] Cancel-in-flight: `cancel task` aborts the active model
  request via `AbortController` (item 4), and now also marks the
  active phase `cancelled` and writes a `phase_cancelled` event.
  `phaseController.markCancelled(reason)` returns the cancelled
  phase name or null when nothing is in progress.
- [x] Alternate-approach flow: when an approval is denied with
  "another approach", `resolvePermission` records a
  `constraint`-typed belief in `beliefs.json` so the model sees
  the constraint on its next iteration. The denied tool result
  already includes `alternative_requested: true` so the model
  notices the change in the same loop.
- [x] Graceful exit: a `SIGINT` handler in `src/index.js` calls
  `cancelTask({ interrupted: true })` and exits with code 130.
  The active phase becomes `interrupted` (distinct from
  `cancelled`) so a future `/resume` can tell user-driven
  cancellations apart from external interruptions.
- [x] `/tasks` command listing recent tasks with status, last
  phase, and a resumable flag. Also `/show <task-id>` for a full
  per-task card.
- [x] Tests: phase controller `markCancelled` / `markInterrupted`
  cases, alternate-approach belief is recorded for both
  command-tier and path-tier denials, and a `/tasks` + `/show`
  end-to-end flow.

### Notes for the next agent

- `/resume <task-id>` intentionally resumes from persisted
  artifacts and reconstructs missing in-memory context when needed.
  It does not try to resurrect a temp shadow workspace after process
  restart; a future hardening slice could persist enough shadow
  metadata to make that possible.

### Definition Of Done

A user can start a long task, hit Ctrl-C, restart the CLI, run `/resume`, and
the harness picks up where it left off without redoing the work it already
finished.

---

## 8. Dependency-Aware Edits And Cross-File Binding

Status: in progress. Cross-file binding, foundational symbol tools,
repair import-graph hints, `dependency_graph(path?)`,
`component_map()`, and signature-change impact have landed;
tsconfig-paths and Python resolution are still open.

### Goal

When the model edits a symbol, surface the call sites it just affected so the
harness can warn the user, run the right targeted checks, and avoid rename
breakage.

### Add

- [~] Cross-file binding: extend `src/code/code-index.js` so imports resolve
  to defining files, not just source-string matches.
  - [x] Shared `src/code/import-resolver.js` resolves relative ESM
        sources against a `Set<string>` of workspace-relative paths,
        with the standard `.ts/.tsx/.js/.jsx/.mjs/.cjs` extension
        list and a `<dir>/index.<ext>` fallback. Bare specifiers
        (`lodash`, `@scope/pkg`) and absolute paths return null.
  - [x] `findSymbolCallers` walks the imports map and only counts a
        file as a caller when its import source resolves to one of
        the symbol's defining files. Honors named imports, aliased
        imports (`import { foo as bar }`), default imports
        (verifies the resolved file declares a default export),
        and namespace imports (`import * as ns; ns.foo()`).
        Reference scanning uses the local alias when present, and
        files that only mention the name in a comment or string
        without importing it are not flagged as callers.
  - [x] `findSymbolDependencies` returns a file's imports with each
        source resolved (or null for bare specifiers), plus
        `internal_count` / `external_count` summary counts.
  - [ ] tsconfig-paths / `paths` alias resolution.
  - [ ] CommonJS `require()` argument resolution.
  - [ ] Python `from x import y` resolution against the package
        layout.
- [~] New tools:
  - [x] `symbol_callers(name)` — wired through
        `tools.findSymbolCallers` and the `symbol_callers` model
        tool (`TOOL_DEFINITIONS` entry + dispatcher case in
        `src/model/openrouter.js`). Triage and patch `allowedTools`
        admit it.
  - [x] `symbol_dependencies(file)` — wired through
        `tools.findSymbolDependencies` and the `symbol_dependencies`
        model tool, with `allowedTools` updated identically.
  - [x] `dependency_graph(path?)` — aggregator across the whole
        workspace, with optional focused path mode. Returns graph
        nodes, resolved internal edges, external imports, and
        summary counts; focused mode includes reachable dependencies
        plus direct dependents that import the path.
  - [x] `component_map()` — React component discovery. Detects
        common function, arrow, wrapped `memo` / `forwardRef`, and
        class component declarations, then links JSX tags inside
        component bodies to local/imported components when the import
        graph can resolve the target file.
- [~] Pre-patch impact analysis: when the model is about to rename or change
  the signature of an exported symbol, the pre-patch plan from item 3
  enumerates affected files and prompts before the change lands.
  - [x] `listSymbolImpact({ codeIndex, symbol })` in
        `src/code/code-index.js` — synchronous import-graph walk
        returning `{ symbol, defining_files, caller_files }` with no
        file reads, suitable for sync planning use.
  - [x] `scanRenameIntent` + `buildRenameImpact` in
        `src/task/pre-patch-plan.js` detect `\brename\b` in the
        user request, extract candidate identifiers (filtering a
        small stopword list), and run them through
        `listSymbolImpact`. The impact list lands as
        `expected_scope.rename_impact` and the affected files are
        added to `candidate_files` so they participate in the same
        danger-zone crosses as keyword-matched files.
  - [x] `rename_impact` warning: `blocking: true` when the symbol
        has any cross-file caller, `severity: "info"` when only the
        definition is affected.
  - [x] Signature-change impact (parameter list / return type
        rewrites) — `scanSignatureIntent` + `buildSignatureImpact`
        detect parameter / argument / signature / return-shape
        requests, extract candidate identifiers, run them through
        `listSymbolImpact`, store `expected_scope.signature_impact`,
        add affected files to candidate scope, and emit
        `signature_impact` warnings.
- [x] Repair-loop hint: pass the resolved import graph of the failing test
  into `model.repair` so the model edits the right source file on the first
  attempt. `summarizeFailureForRepair(parsed, { codeIndex })` calls
  `findSymbolDependencies({ codeIndex, file })` for each indexed
  failed file, keeps only internal resolved imports, and attaches
  `import_graph: { <test-file>: [<resolved>...] }` on the summary.
  `src/verify/repair-loop.js` threads the runtime's cached code
  index into the summarizer before invoking `model.repair`. Covered
  by tests in `test/failure-summary.test.js` and
  `test/repair-loop.test.js`.
- [x] Tests with imports across folders, default vs named imports, and
  files that only mention the name without importing it
  (`test/import-resolver.test.js` — 7 tests;
  `test/symbol-callers.test.js` — 9 tests). Component-map tests in
  `test/code-index.test.js` cover React component discovery, render
  edges, and runtime exposure. Signature-impact tests in
  `test/pre-patch-plan.test.js` cover blocking cross-file callers,
  informational local-only impact, unmatched symbols, and non-
  signature requests. Re-export and dynamic-import coverage is open.

### Notes for the next agent

- `findSymbolCallers` deliberately resolves the import source
  through `resolveImport`, not by string equality with the
  defining file's path. That's what makes it possible to import
  `login` from `../auth/login` in one file and `login` from
  `../billing/login` in another and have each caller correctly
  attributed to its own definition file (see the third test in
  `test/symbol-callers.test.js`).
- For namespace imports, `importLocalName` returns the namespace
  name (`ns`) rather than the symbol's name. Reference scanning
  then matches `ns.<symbol>` rather than a bare `<symbol>(...)`,
  which avoids false positives on unrelated locals.
- The shared resolver's signature accepts either a `Set` or any
  iterable for `fileSet`; `findSymbolCallers` builds a Set once
  per call and reuses it across all imports in the workspace.
- `src/checks/relevant-files.js` was migrated to import
  `resolveImport` from the shared module. The previous
  `resolveRelativeImport` is now a one-line delegate so existing
  callers (and their tests) keep working unchanged.
- `dependency_graph(path?)` is intentionally file-level for now. It
  does not understand tsconfig aliases, CommonJS `require()`,
  dynamic imports, re-exports, or Python imports until the resolver
  grows those capabilities.
- `component_map()` is best-effort and regex-based. It does not parse
  anonymous default exports, HOC chains beyond simple `memo` /
  `forwardRef`, polymorphic lower-case components, or exact JSX
  control flow.
- `signature_impact` is intent-based and symbol-level. It does not
  parse actual before/after function signatures; it warns when the
  request language makes a signature change likely and a mentioned
  workspace symbol has known callers.

### Definition Of Done

The harness can answer "if I rename `updateUser`, what files are affected?"
with file-level evidence, and it surfaces that answer to both the model and
the user before patching.

---

## 9. Real Language-Server Integration (Optional, TypeScript First)

Status: not started.

### Goal

For TypeScript and JavaScript projects with `tsconfig.json`, replace the
regex code intel with real type-aware navigation when a language server is
available.

### Add

- [ ] Optional dependency on `typescript` and a small wrapper that drives
  `tsserver` (or `typescript-language-server`) over stdio.
- [ ] LSP-backed implementations of `find_definition`, `find_references`,
  `find_symbols`, and `find_imports` that fall back to the regex index when
  the LSP is unavailable or rejects a request.
- [ ] New LSP-only tools: `hover_info`, `signature_help`, and
  `diagnostics(path)` for live type errors without running `tsc`.
- [ ] Workspace lifecycle: spin the LSP up at the start of a task, share it
  across tools in the same task, shut it down when the task ends. Honor the
  shadow workspace path so the LSP types match the workspace the model is
  editing.
- [ ] Feature flag: `code_intel.lsp = "off" | "auto" | "on"` in
  `.agent/config.json`, default `"auto"` (try LSP, fall back to regex).
- [ ] Capability tests verifying `hover_info` matches what the index returned
  for the same symbol, and that the LSP path agrees with the regex path on
  every fixture.
- [ ] Future hooks: clearly-isolated module so a Python LSP (`pyright`,
  `pylsp`) can be added next without restructuring.

### Definition Of Done

On a TypeScript fixture, the harness answers "where is `updateUser` defined,
who calls it, what type does it return?" using `tsserver` evidence — and on a
non-TypeScript fixture, falls back to the existing regex index transparently.

---

## 10. GitHub/CI Integration

Status: not started.

### Goal

Bring the agent up to where it can branch, push, open PRs, watch CI, and
repair from CI logs — under the same approval boundaries that protect every
other risky action.

### Add

- [ ] `gh` CLI detection and a thin wrapper module
  (`src/integrations/github.js`).
- [ ] New tools (each gated by the existing approval engine):
  - `branch_create(name)` — creates a branch from the current HEAD with a
    checkpoint event.
  - `pr_create({ title, body })` — opens a PR via `gh pr create`, records
    `pr.json` on the task.
  - `pr_status(number?)` — fetches checks for the active PR.
  - `ci_log(run_id, job?)` — streams the failed-job log so the repair loop
    can see what broke.
- [ ] CI repair flow: when a `pr_status` shows failures, parse the CI log
  through the same parsers from item 2 and feed structured failures into
  `model.repair` like a local check failure.
- [ ] Branch and push are classified as `external_publish` (already exists in
  the permission engine) so they always prompt; force-push is blocked.
- [ ] Review action: a "open PR" choice on the final review card that runs
  the branch + push + `pr_create` flow with a single approval prompt.
- [ ] Tests using a mocked `gh` shim under `test/fixtures/github/` so the
  flow can be exercised without contacting GitHub.

### Definition Of Done

After accepting a task, a user can answer the prompt "open a PR for this?"
and the harness creates a branch, pushes it, opens a PR, and watches the
checks. If CI fails, the same repair loop that handles local failures takes
the CI log as its input.

---

## Recommended Build Order

1. Real-repo fixtures and end-to-end CLI coverage. Everything else is easier
   to verify once this exists.
2. Stronger targeted checks and parsers. Uses fixtures from item 1; produces
   structured failures other items depend on.
3. Pre-patch blast radius and edit preview. Uses fixtures and parsers; lays
   the groundwork for safer multi-file edits.
4. Streaming wired into the CLI. Self-contained productization win that does
   not depend on items 5–10.
5. Multi-provider model adapters. Capability flags and adapter contract are
   already in place; this is mostly new code behind the existing factory.
6. Structured outputs beyond critique. Builds on items 3 and 5 to produce the
   plan, edit-spec, and repair-finding schemas.
7. Resumable tasks and cancel-in-flight. Easier once streaming and structured
   outputs exist; touches `src/index.js` and the phase controller.
8. Dependency-aware edits and cross-file binding. Strengthens code-index.js
   without requiring an external LSP.
9. Real language-server integration. Optional and heavier; depends on
   stable cross-file binding and pre-patch plan to compare against.
10. GitHub/CI integration. Sits on top of the parsers (CI log → repair) and
    benefits from streaming, structured outputs, and resumable tasks.

---

## Phase 3 Definition Of Done

Phase 3 is done when the user can open a real existing repository and say:

```txt
Fix the failing build and open a PR
```

The agent should:

- [x] resume the task safely if the user restarts the CLI mid-flight
- [ ] stream its narration as it works
- [ ] use a real test/build/lint reporter to extract structured failures
- [ ] use cross-file binding (or an LSP when available) to pick the right
      source files
- [ ] produce a pre-patch blast radius the user can preview
- [ ] use structured outputs for the plan and edit spec
- [ ] support whichever provider the user configured (OpenRouter, Anthropic,
      OpenAI, or a local model)
- [ ] run targeted checks, parse failures, and repair within the bounded loop
- [ ] open a PR through the approval engine when the user accepts
- [ ] watch CI and repair from CI logs through the same loop
- [ ] keep all the Phase 2 safety guarantees: shadow workspace, apply-back
      conflict resolution, snapshot undo, permission boundaries, audit logs,
      and reviewable artifacts

Remaining gaps before Phase 3 is fully done:

- All ten items above.
