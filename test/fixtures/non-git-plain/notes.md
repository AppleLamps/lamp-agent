# Project Notes

This is a deliberately minimal directory used by the lamp-agent end-to-end
suite. It has no git history, no package.json, and no test runner so the
harness exercises the snapshot-diff path and the no-package-manager fallback.
